🇨🇳 对于国内开发者推荐顺序：

| **优先级** | **工具**         | **推荐理由**         |
| ------- | -------------- | ---------------- |
| 1       | **pnpm**       | 安装速度快，磁盘省，现代项目友好 |
| 2       | **yarn**       | 缓存快、稳定，主流框架支持好   |
| 3       | **npm + 淘宝镜像** | 兼容最好，适合保守项目      |
| 4       | **bun**        | 仅供尝鲜，生态尚不成熟      |

# npm - 传统老大哥

npm是node.js默认的包管理系统，在安装node的时候会一起安装，用于管理node中的第三方插件。

## npm与npx

|功能|`npm`|`npx`|
|:--|:--|:--|
|安装包|✅ `npm install`|❌ 不安装|
|全局运行命令|✅ 需先 `npm install -g`|✅ 直接运行|
|临时运行命令|❌ 不支持|✅ 支持|
|避免污染全局环境|❌ 会安装到全局|✅ 不污染全局|

## npm镜像源切换

```sh
npm config get registry # 查看当前镜像
npm install -g nrm
nrm ls            # 查看可用镜像
  npm ---------- https://registry.npmjs.org/
  yarn --------- https://registry.yarnpkg.com/
  tencent ------ https://mirrors.tencent.com/npm/
  cnpm --------- https://r.cnpmjs.org/
* taobao ------- https://registry.npmmirror.com/
  npmMirror ---- https://skimdb.npmjs.com/registry/
  huawei ------- https://repo.huaweicloud.com/repository/npm/
nrm use taobao   # 切换到淘宝源
```

## npm多版本切换

nvm即`node version manager`，好处是方便切换node.js的版本。

安装注意事项

1. 要卸载现有的node.js
2. 提示选择nvm和node.js目录时，一定要<font color="#f79646">避免目录中出现空格</font>
3. 选用<font color="#f79646">以管理员身份运行cmd程序</font>，在cmd程序中执行nvm的命令
4. 首次运行前设置好国内镜像地址

```
nvm node_mirror http://npm.taobao.org/mirrors/node
nvm npm_mirror http://npm.taobao.org/mirrors/npm
```

* [win11安装](https://juejin.cn/post/7119787443519225886)
* [nvm安装、卸载与使用详情](https://juejin.cn/post/7000652162950758431)

1.首先查看有哪些可用版本

```sh
nvm list # 查看本地
->     v22.16.0                        # 当前激活的版本
        v23.9.0                        # 本地已安装但未激活的版本
default -> node (-> v23.9.0)           # 表示你每次打开shell时自动使用的版本，可通过nvm alias default 22.16.0修改
iojs -> N/A (default)                  # iojs是Node.js早期一个分支，现在已被合并，不常用了，N/A表示本地没有安装iojs
unstable -> N/A (default)              # unstable是一个旧别名，指向开发版，也未安装
node -> stable (-> v23.9.0) (default)  # node和stable都是别名，指向稳定版本v23.9.0
stable -> 23.9 (-> v23.9.0) (default)
lts/* -> lts/jod (-> v22.16.0)         # 当前LTS版本别名是jod，版本为v22.16.0
lts/argon -> v4.9.1 (-> N/A)
lts/boron -> v6.17.1 (-> N/A)
lts/carbon -> v8.17.0 (-> N/A)
lts/dubnium -> v10.24.1 (-> N/A)
lts/erbium -> v12.22.12 (-> N/A)
lts/fermium -> v14.21.3 (-> N/A)
lts/gallium -> v16.20.2 (-> N/A)
lts/hydrogen -> v18.20.8 (-> N/A)
lts/iron -> v20.19.2 (-> N/A)
lts/jod -> v22.16.0

nvm ls-remote 
```

>建议安装LTS（长期支持版）

```sh
$ nvm install 20.11.1

$ nvm use 20.11.1
Now using node v20.11.1 (64-bit)

$ npm -v
10.2.4

$ node -v
v20.11.1
```

|             |                           |
| ----------- | ------------------------- |
| 安装 LTS 最新版本 | nvm install --lts         |
| 切换版本        | nvm use 22.16.0           |
| 设置默认版本      | nvm alias default 22.16.0 |
| 卸载版本        | nvm uninstall 23.9.0      |
| 查看远程可用版本    | nvm ls-remote             |

# pnmp **🚀 推荐使用**




# 搭建前端服务器

```sh
# TODO
```