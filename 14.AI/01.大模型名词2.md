# 重点

[飞天闪客 - 一口气拆穿 Skill / MCP / RAG / Agent / OpenClaw 底层逻辑](https://www.bilibili.com/video/BV1ojfDBSEPv/?spm_id_from=333.337.search-card.all.click&vd_source=401e9151ff5196d99069159680a48dbc)
	
## 1. LLM 大语言模型

## 2. Prompt 提示词

## 3. Context 上下文

## 4. Memory 记忆

## 5. Agent 智能体
 
## 6. RAG 检索增强生成

## 7. Search 搜索

![[Pasted image 20260403212858.png|L|800]]

## 8. Tools 工具

指 Agent 或大模型可调用的外部能力，比如搜索、计算、文件操作、数据库查询、API 调用等。`Tools` 解决的是“有哪些能力可以用”，而 `Function Calling` 解决的是“模型如何把这些能力调用起来”。

![[Pasted image 20260403211538.png|L|800]]

如果工具是通过 MCP 接入的，那么 Agent / client 就可以按统一协议去发现和调用它们，例如 `tools/list` 用来返回工具列表，`tools/call` 用来调用具体工具，这套协议叫做 **MCP**，全称是 **Model Context Protocol**，中文常译为**模型上下文协议**。

![[Pasted image 20260403212518.png|L|800]]

但要注意：`Function Calling / Tool Calling` 并不依赖 MCP。即使没有 MCP，应用也可以把本地函数、脚本或第三方 API 直接写进自己的代码里，让模型按约定格式“申请调用”，再由外部程序真正执行。

可以这样通俗解释：

Function/Tool Calling 解决的是：  
“模型要不要用工具？如果要，用哪个工具、传什么参数？”

MCP 解决的是：  
“这些工具用什么统一方式接进来，方便不同客户端复用？”

最容易懂的类比是：

- Function Calling 像“点菜”  
    你告诉服务员：我要鱼香肉丝，一份，不辣。
- MCP 像“外卖平台的统一接单规范”  
    商家按平台规则接单、出单、回传状态。

所以“不用 MCP 也能做 function/tool calling”的意思就是：

你不一定非要上“外卖平台”才能点菜。  
你也可以直接给饭店打电话下单。

放到程序里就是：

1. 你自己在代码里手写一个工具 get_weather(city)
2. 把这个工具描述告诉模型
3. 模型返回：调用 get_weather("北京")
4. 你的程序自己去执行这个函数
5. 再把结果喂回模型

这里整个过程已经是 function calling 了，但完全没有 MCP server、没有 tools/list、也没有 tools/call。

一个很直白的对比：

- 有 MCP：工具像“标准插头”，客户端按统一协议去发现和调用
- 没有 MCP：工具像“你自己焊的私有接口”，照样能用，只是不通用

你可以在文档里写成一句更顺的版本：

> Function/Tool Calling 只要求模型能用结构化格式表达“我要调用哪个工具、参数是什么”；至于这些工具是通过 MCP 接入，还是由应用自己硬编码接入，都可以。

## 9. Function Calling

可以，把 **Function Calling** 理解成：

> 让大模型在需要时，按约定格式“请求调用外部函数/工具”的机制。

它不是模型自己真的去执行代码，而是：

1.	模型判断：当前问题是否需要调用某个工具
2.	模型产出：一个结构化的“函数调用请求”
3.	Agent 执行：真正去调用后端函数 / API / 数据库 / 搜索工具
4.	把结果回传给模型
5.	模型再基于结果生成最终回答

### 为什么会有 Function Calling

普通聊天模型只会“生成文本”。

例如你问：

>“帮我查一下北京今天天气”

如果只是纯文本模型，它可能会“猜”一个天气。
但真实场景里，天气应该来自 天气 API，不是模型瞎编。

所以就需要一种机制，让模型在合适的时候说：

>“这事我该调用 get_weather(city=北京) 这个函数。”

这就是 Function Calling 的核心价值：

•	让模型能连接外部世界
•	让回答基于真实数据
•	让输出参数结构化，方便 Agent 处理
•	适合做 Agent、助手、自动化流程

### 它到底是什么

Function Calling 本质上是：

> 你先告诉模型：我这里有哪些函数可用，每个函数需要什么参数。

然后模型在对话过程中，可能会返回类似这样的内容：

```json
{
  "name": "get_weather",
  "arguments": {
    "city": "北京"
  }
}
```

注意：这不是函数已经执行了。

这只是模型在说：

> “我建议调用这个函数，并且参数是这些。”

真正执行的是你的 Agent，不是模型本身。

### 一个完整的流程

比如你有一个函数：

```python
def get_weather(city: str) -> str:
    return "北京今天晴，18~26°C"
```

用户问：

北京今天天气怎么样？

流程如下：

**第一步：把函数描述告诉模型**

你告诉模型：
	•	有个函数叫 get_weather
	•	参数是 city
	•	用来查询天气

**第二步：模型决定调用函数**

模型返回：

```json
{
  "name": "get_weather",
  "arguments": {
    "city": "北京"
  }
}
```

**第三步：Agent 收到这个请求后，真正执行函数**

```python
result = get_weather("北京")
```

得到：

```text
北京今天晴，18~26°C
```

**第四步：把函数结果再发给模型**

你把结果告诉模型：

>工具调用结果：北京今天晴，18~26°C

**第五步：模型组织成自然语言回复用户**

最后模型输出：

>北京今天天气晴，气温大约 18 到 26 摄氏度。

### 一句话总结

> **Function Calling 就是：让大模型用结构化方式“申请调用工具”，再由外部程序真正执行，并把结果返回给模型继续回答。**


## 10. MCP

![[Pasted image 20260321005753.png|L|800]]

## 11. LangChain

![[Pasted image 20260321010534.png|L|800]]

## 12. Workflow（工作流）

为了应对非程序员场景，可通过低代码平台以简单拖拽的方式搭建工作流，实现任务编排。

![[Pasted image 20260321010720.png|L|800]]

## 13. Skill

![[Pasted image 20260321010916.png|L|800]]

![[Pasted image 20260321011008.png|L|800]]

## 14. SubAgent

![[Pasted image 20260321011057.png|L|800]]

# 易混淆 1：Function Calling 与 MCP 的区别

### Function Calling 的关注点

它关注的是：

> **“这一轮对话里，模型该怎么表达一次工具调用意图？”**

也就是偏 **推理时的调用动作**。

模型会产出类似：

```json
{
  "name": "get_weather",
  "arguments": {
    "city": "Beijing"
  }
}
```

然后由应用侧执行。

### MCP 的关注点

它关注的是：

> **“外部工具/数据源，如何以统一协议接入，让不同 AI 客户端都能用？”**

也就是偏 **系统集成 / 接口标准化**。

MCP 不只是“调用函数”，还定义了：

- 怎么列出工具
- 怎么暴露资源
- 怎么提供 prompt 模板
- 客户端和 server 怎么通信

官方把它描述成连接 AI 应用与外部系统的开放标准。

### 一个更精准的分层图

```text
用户
  ↓
Agent / LLM
  ↓  （决定要不要调工具：Function Calling / Tool Calling）
工具调用层
  ↓
工具接入层
  ├─ 本地函数
  ├─ 内置工具
  ├─ 第三方 API
  └─ MCP Server <--- 在此 Agent 作为 client 去发现这些能力
        ├─ Tools
        ├─ Resources
        └─ Prompts
```

- **Function Calling** 在“工具调用层”
- **MCP** 在“工具接入层/协议层”

### 案例 - Claude Code MCP

#### 安装 / 接入准备阶段
 
``` plantuml
@startuml
autonumber

actor 用户 as U
participant "Claude Code" as CC
participant "Claude Code MCP配置\n(.mcp.json / <U+007E>/.claude/settings.json)" as CFG
participant "MySQL MCP Server" as MCP
database "MySQL" as DB

U -> U : 选择一个 MySQL MCP Server 实现

note right of U
安装的不是“协议本身”，
而是一个实现了 MCP 协议的
MySQL MCP Server 进程
end note

U -> CFG : 配置 MCP Server 启动信息

note right of CFG
Claude Code 的 MCP 配置里常见字段：

1. command
   - 用来启动 MCP Server 的命令
   - 例如：npx / python / uvx

2. args
   - 传给 command 的参数数组
   - 例如：["-y", "@your-org/mysql-mcp-server"]

3. env
   - 启动 MCP Server 时注入的环境变量
   - 常用于放数据库连接信息

一个示例：

{
  "mcpServers": {
    "mysql": {
      "command": "npx",
      "args": ["-y", "@your-org/mysql-mcp-server"],
      "env": {
        "MYSQL_HOST": "10.2.2.10",
        "MYSQL_PORT": "3306",
        "MYSQL_DATABASE": "hospital",
        "MYSQL_USER": "readonly_bot",
        "MYSQL_PASSWORD": "******"
      }
    }
  }
}
end note

U -> CC : 启动 Claude Code / 打开项目
CC -> CFG : 读取 MCP 配置
CFG --> CC : 返回 mysql server 配置

CC -> MCP : 按 command + args 启动 MCP Server

note over CC,MCP
本地 MCP Server 常见通过 stdio 方式连接
Claude Code 负责拉起进程并建立 MCP client 连接
end note

MCP -> MCP : 读取 env 中的数据库连接信息
MCP -> DB : 使用 host/port/user/password/database 连接 MySQL

alt 数据库连接成功
    DB --> MCP : 连接成功
    MCP --> CC : 注册成功，暴露 tools
else 数据库连接失败
    DB --> MCP : 连接失败
    MCP --> CC : 返回错误
    CC --> U : MySQL MCP Server 已启动，但数据库连接失败
end

note over MCP,CC
MCP Server 暴露的 tools 可能包括：
- list_tables
- describe_table
- query
具体名称取决于 Server 实现
end note

@enduml
```
#### 运行时调用阶段

```plantuml
@startuml
autonumber

actor 用户 as U
participant "Claude Code" as CC
participant "MySQL MCP Server" as MCP
database "MySQL" as DB

U -> CC : 查询 hospital 库今天未执行的护理医嘱数量

note over CC
Claude 先看当前有哪些 tools 可用，
再决定是否发起 tool use / function calling
end note

CC -> MCP : tool call: list_tables(database="hospital")
MCP -> DB : SHOW TABLES FROM hospital
DB --> MCP : 表列表
MCP --> CC : 表列表结果

CC -> MCP : tool call: describe_table(table="pat_inhos_order")
MCP -> DB : DESCRIBE pat_inhos_order
DB --> MCP : 字段结构
MCP --> CC : 字段结构结果

CC -> MCP : tool call: query("SELECT COUNT(*) ...")
MCP -> DB : 执行只读 SQL
DB --> MCP : 查询结果
MCP --> CC : 结构化结果

CC --> U : 今天未执行的护理医嘱共有 N 条

@enduml
```


# 易混淆 2：Skill 与 MCP 的区别



# 总结

![[Pasted image 20260321011231.png|L|400]]
