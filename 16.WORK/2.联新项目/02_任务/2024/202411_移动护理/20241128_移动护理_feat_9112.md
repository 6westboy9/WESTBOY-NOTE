# 1.CR

>Java群

项目：mnis  
内容：9112【河南科技大学第一附属医院（开元院区）】PC端评估单需评第一页低于第二页第一条时间不让评估，需要放开权限换页后能需评前面的内容
分支：feat_release_mnis_v3.8_wpb_9112 into qa_mnis_v3.8
MR：http://10.2.3.111:86/backend_group/backend_application/mnis/merge_requests/6149

# 2.脚本

>邮件

> [!danger] 注意事项
> 提供脚本为实际线上脚本，所以必须考虑实际线上情况
> 1.开关，默认应为关闭（根据实际情况而定）
> 2.IP地址，默认应为127.0.0.1（一般情况）

---

宝哥麻烦归档下脚本：

测试环境：10.2.3.170
项目：mnis
冲刺版本：移动护理迭代56
单号：9122
版本号：MNIS V3.8.118
分支：feat_release_mnis_v3.8_wpb_9112

MySQL脚本

```js
use coms
db.sysConfigFile.find({'key': 'docsLimitsSetting' })
db.sysConfigFile.update({ 'key': 'docsLimitsSetting' }, { "$pull": { 'children': { 'key': { $in: ['evalFormPrePageDataHasTimeLimit'] } } } })
db.sysConfigFile.update({ 'key': 'docsLimitsSetting' }, {
    "$push": {
        "children":
        {
            $each: [
   		{
			"_id" : "17327893627021",
			"key" : "evalFormPrePageDataHasTimeLimit",
			"pattern" : {
				"componentType" : "Switch",
				"label" : "评估单录入换页前的数据是否有时间限制"
			},
			"value" : false
		}
            ]
        }
    }
})
```


> [!danger] 注意事项
> 脚本后不要带 `;` ~

MongDB脚本

```js

```

# 3.提测单

>提测群

http://10.2.3.109:1024/zentao/testtask-view-8680.html
9112【河南科技大学第一附属医院（开元院区）】PC端评估单需评第一页低于第二页第一条时间不让评估，需要放开权限换页后能需评前面的内容
# 4.其它
