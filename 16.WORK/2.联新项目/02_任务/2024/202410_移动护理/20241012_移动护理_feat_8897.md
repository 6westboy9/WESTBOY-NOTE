# 1.CR

>Java群

项目：mnis  
内容：8897【湛江中心人民医院】手术室医嘱执行（涉及科室统计
分支：feat_release_mnis_v3.8_wpb_8897 into qa_mnis_v3.8
MR：

# 2.脚本

>邮件

> [!danger] 注意事项
> 提供脚本为实际线上脚本，所以必须考虑实际线上情况
> 1.开关，默认应为关闭（根据实际情况而定）
> 2.IP地址，默认应为127.0.0.1（一般情况）

---

宝哥麻烦归档下脚本：

测试环境：10.2.3.170
项目：mnis
服务：mnis
冲刺版本：
问题单号：
版本号：
分支：

MySQL脚本

```sql

```

> [!danger] 注意事项
> 脚本后不要带 `;` ~

MongDB脚本

```js
use coms
db.sysConfigFile.updateOne({ 'key': 'orderExecutionConfig' }, { "$pull": { 'children': { 'key': { $in: ['operatingRoomCfgByFuzzyOrderRemark', 'operatingRoomCfgByFuzzyOrderUsage'] } } } })
db.sysConfigFile.updateOne({ 'key': 'orderExecutionConfig' }, {
    "$push": {
        "children":
        {
            $each: [{
                "_id": "17287154873493",
                "key": "operatingRoomCfgByFuzzyOrderRemark",
                "pattern": {
                    "description": "请输入医嘱备注关键词，用“,”分隔",
                    "componentType": "Input",
                    "label": "手术室执行病区医嘱配置（模糊匹配医嘱备注）"
                }
            },
            {
                "_id": "17287154842012",
                "key": "operatingRoomCfgByFuzzyOrderUsage",
                "pattern": {
                    "description": "请输入医嘱用法关键词，用“,”分隔",
                    "componentType": "Input",
                    "label": "手术室执行病区医嘱配置（模糊匹配医嘱用法）"
                }
            }]
        }
    }
})
```

# 3.提测单

>提测群



 
 已提测
# 4.其它
