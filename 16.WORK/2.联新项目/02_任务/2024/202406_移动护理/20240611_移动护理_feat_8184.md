# 1.CR

>Java群

项目: foundation  
内容: 8184【烟台市口腔医院】体温单批量录入界面有个“打印手写单”的按钮，打印时大便小便次数需要显示在15点时段，而非07和19点时段
分支: feat_master_wpb_8184 into qa
MR: http://10.2.3.111:86/backend_group/backend_application/foundation/merge_requests/693

项目: foundation_fastdeploy 
内容: 8184【烟台市口腔医院】体温单批量录入界面有个“打印手写单”的按钮，打印时大便小便次数需要显示在15点时段，而非07和19点时段
分支: feat_master_wpb_8184 into qa
MR: http://10.2.3.111:86/backend_group/backend_application/foundation_fastdeploy/merge_requests/1319

# 2.脚本

>邮件

```ad-error
注意提供脚本为实际<font color="#9bbb59">上线脚本</font>！

注意配置相关的默认值
 1. 开关，默认应为关闭
 2. IP地址，默认应为127.0.0.1
```

---

宝哥麻烦归档下脚本：

测试环境：10.2.3.170
项目：mnis
服务：mnis
冲刺版本：移动护理迭代45
故事编号：8184
版本号：MNIS V3.8.101
分支：feat_release_mnis_v3.8_wpb_8184

MySQL脚本

```sql
-- 无
```


```ad-error
注意脚本后不要带 `;` ~
```

MongDB脚本

```js
use coms
db.sysConfigFile.updateOne({ 'key': 'batchInputSet' }, { "$pull": { 'children': { 'key': { $in: ['printModeExcretionPosition'] } } } })
db.sysConfigFile.updateOne({ 'key': 'batchInputSet' }, {
    "$push": {
        "children":
        {
            $each: [{
			"_id" : "17180726917541",
			"key" : "printModeExcretionPosition",
			"pattern" : {
				"description" : "默认未选任何选项时为末列展示",
				"componentType" : "MultiSelect",
				"label" : "打印模式时二便展示位",
				"urlOptionSourceDetail" : {
					"keyField" : "vitalsign",
					"resultKey" : "vitalsignBaseConfig",
					"url" : "/sys/sysConfigFileExt/valueV2?key=vitalsignBaseConfig"
				},
				"optionSourceDetail" : "",
				"optionSource" : "url",
			},
			"value":[]
		}]
        }
    }
})
```

# 3.提测单

>提测群

http://10.2.3.109:1024/zentao/testtask-view-7764.html
8184【烟台市口腔医院】体温单批量录入界面有个“打印手写单”的按钮，打印时大便小便次数需要显示在15点时段，而非07和19点时段 --- 已提测
# 4.其它

