## 1.CR

>Java群

项目: mnis  
内容: 8241【佛山市中医院】医嘱执行-第二瓶补液执行时自动结束上一瓶未拔针的输液，无需手动勾选确认
分支: feat_release_mnis_v3.8_wpb_8241 into qa_mnis_v3.8
MR: http://10.2.3.111:86/backend_group/backend_application/mnis/merge_requests/5340

# 2.脚本

>邮件

> [!danger] 注意事项
> 注意1.提供脚本为实际<font color="#9bbb59">线上脚本</font>
> 注意2.配置相关的默认值
>   - 开关，默认应为关闭
>   - IP地址，默认应为127.0.0.1

---

宝哥麻烦归档下脚本：

测试环境：10.2.3.170
项目：mnis
服务：mnis
冲刺版本：移动护理迭代46
故事编号：8241
版本号：MNIS V3.8.102
分支：feat_release_mnis_v3.8_wpb_8241

MySQL脚本

```sql
-- 无
```

> [!danger] 
> 注意脚本后不要带 `;` ~

MongDB脚本

```js
use coms
db.sysConfigFile.updateOne({ 'key': 'orderExecutionConfig' }, { "$pull": { 'children': { 'key': { $in: ['autoEndPreviousInfusionOrder', 'autoEndPreviousTransfusionOrder'] } } } })
db.sysConfigFile.updateOne({ 'key': 'orderExecutionConfig' }, {
    "$push": {
        "children":
        {
            $each: [{
                "_id": "17186985436741",
                "key": "autoEndPreviousInfusionOrder",
                "pattern": {
                    "componentType": "Switch",
                    "label": "执行开始时是否自动结束上一瓶输液医嘱"
                },
                "value": false
            }, {
                "_id": "17186985436741",
                "key": "autoEndPreviousTransfusionOrder",
                "pattern": {
                    "componentType": "Switch",
                    "label": "执行开始时是否自动结束上一瓶输血医嘱"
                },
                "value": false
            }]
        }
    }
})
```

# 3.提测单

>提测群

http://10.2.3.109:1024/zentao/testtask-view-7795.html
8241【佛山市中医院】医嘱执行-第二瓶补液执行时自动结束上一瓶未拔针的输液，无需手动勾选确认 --- 已提测
# 4.其它

