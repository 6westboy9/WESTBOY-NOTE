## 1.CR

>Java群

项目: mnis  
内容: 8046【湖南省妇幼保健院】统计出入量 - 自定义时间段，右键同步至体温单时，无法带入时间
分支: feat_release_mnis_v3.8_wpb_8046 into qa_mnis_v3.8
MR: http://10.2.3.111:86/backend_group/backend_application/mnis/merge_requests/5324

# 2.脚本

>邮件

> [!danger] 注意事项
> 注意1.提供脚本为实际<font color="#9bbb59">线上脚本</font>
> 注意2.配置相关的默认值
>   - 开关，默认应为关闭
>   - IP地址，默认应为127.0.0.1

---

宝哥麻烦归档下脚本：

测试环境：10.2.3.170
项目：mnis
服务：mnis
冲刺版本：移动护理迭代45
故事编号：8046
版本号：MNIS V3.8.101
分支：feat_release_mnis_v3.8_wpb_8046

MySQL脚本

```sql
-- 无
```

> [!danger] 
> 注意脚本后不要带 `;` ~

MongDB脚本

```js
use coms
db.sysConfigFile.updateOne({ 'key': 'skinTestOfTemperatureFormConfig' }, { "$pull": { 'children': { 'key': { $in: ['customizedInputOutPutFormat'] } } } })
db.sysConfigFile.updateOne({ 'key': 'skinTestOfTemperatureFormConfig' }, {
    "$push": {
        "children":
        {
            $each: [{
                "_id": "65095edbcf1fe2309d010608-Mon May 27 2024 17:34:14 GMT+0800",
                "key": "customizedInputOutPutFormat",
                "pattern": {
                    "description": "",
                    "componentType": "Radio",
                    "label": "自定义时间段出入量同步格式",
                    "urlOptionSourceDetail": {

                    },
                    "optionSourceDetail": "",
                    "optionSource": "options",
                    "options": [
                        {
                            "sortedIndex": 1,
                            "code": "total",
                            "name": "总量值"
                        },
                        {
                            "sortedIndex": 2,
                            "code": "hourMinTotal",
                            "name": "H*min+总量值"
                        },
                        {
                            "sortedIndex": 3,
                            "code": "hourTotal",
                            "name": "总量值/H"
                        }
                    ]
                },
                "value": "total"
            }]
        }
    }
})
```

# 3.提测单

>提测群

http://183.62.162.28:1024/zentao/testtask-view-7768.html
8046【湖南省妇幼保健院】统计出入量 - 自定义时间段，右键同步至体温单时，无法带入时间 --- 已提测
# 4.其它

