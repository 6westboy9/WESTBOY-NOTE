## 1.CR

>Java群

项目：mnis  
内容：8622.NDA端【昆山市第一人民医院】执行单界面改成原来的样式，全部展开，方便护士交接班
分支：feat_release_mnis_v3.8_wpb_8622 into qa_mnis_v3.8
MR：

# 2.脚本

>邮件

> [!danger] 注意事项
> 提供脚本为实际线上脚本，所以必须考虑实际线上情况
> 1.开关，默认应为关闭（根据实际情况而定）
> 2.IP地址，默认应为127.0.0.1（一般情况）

---

宝哥麻烦归档下脚本：

测试环境：10.2.3.170
项目：mnis
服务：mnis
冲刺版本：移动护理迭代50
故事编号：8622
版本号：MNIS V3.8.111
分支：feat_release_mnis_v3.8_wpb_8622

MySQL脚本

```sql
-- 
```

> [!danger] 注意事项
> 脚本后不要带 `;` ~

MongDB脚本

```js
use coms
db.sysConfigFile.updateMany({ 'key': 'orderExecuteConfig' }, { "$pull": { 'children': { 'key': { $in: ['isPdaExecutionOrderExpanded'] } } } })
db.sysConfigFile.updateMany({ 'key': 'orderExecuteConfig' }, {
    "$push": {
        "children":
        {
            $each: [{
                "_id": "65095edacf1fe2309d010513-Thu Aug 15 2024 13:39:30 GMT+0800",
                "key": "isPdaExecutionOrderExpanded",
                "pattern": {
                    "componentType": "Switch",
                    "label": "PDA执行单是否展开"
                },
                "value": false
            }]
        }
    }
})
```

# 3.提测单

>提测群


8622.NDA端【昆山市第一人民医院】执行单界面改成原来的样式，全部展开，方便护士交接班 --- 已提测

# 4.其它








