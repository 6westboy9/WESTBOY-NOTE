>原始需求：[[20240625_移动护理_feat_8260_8月份8260重启]]
## 1.CR

>Java群

项目：mnis  
内容：8260【规划】备血时，用血编号为空
分支：feat_release_mnis_v3.8_wpb_8260 into qa_mnis_v3.8
MR：http://10.2.3.111:86/backend_group/backend_application/mnis/merge_requests/5559

项目：emr  
内容：8260【规划】备血时，用血编号为空
分支：feat_release_mnis_v3.8_wpb_8260 into qa_mnis_v3.8
MR：http://10.2.3.111:86/backend_group/backend_application/emr/merge_requests/2568
# 2.脚本

>邮件

> [!danger] 注意事项
> 提供脚本为实际线上脚本，所以必须考虑实际线上情况
> 1.开关，默认应为关闭（根据实际情况而定）
> 2.IP地址，默认应为127.0.0.1（一般情况）

---

宝哥麻烦归档下脚本：

测试环境：10.2.3.170
项目：mnis
服务：mnis
冲刺版本：移动护理迭代50
故事编号：8260
版本号：MNIS V3.8.111
分支：feat_release_mnis_v3.8_wpb_8260

MySQL脚本

```sql
use windranger_emr;
CREATE TABLE `pat_emr_blood_prepare` (
  `seq_id` bigint(20) unsigned NOT NULL AUTO_INCREMENT COMMENT '自增长流水号',
  `unique_code` varchar(50) NOT NULL DEFAULT '' COMMENT '唯一标识',
  `inhos_code` varchar(20) NOT NULL DEFAULT '' COMMENT '患者住院编号',
  `pat_code` varchar(20) NOT NULL COMMENT '病人标识号',
  `apply_dept_code` varchar(20) DEFAULT '' COMMENT '申请科室编号',
  `apply_dept_name` varchar(20) DEFAULT '' COMMENT '申请科室名称',
  `pat_blood_type` varchar(20) DEFAULT '' COMMENT '患者ABO血型',
  `pat_rh_type` varchar(20) DEFAULT '' COMMENT '患者rh血型',
  `blood_component_type` varchar(20) DEFAULT '' COMMENT '输血成分',
  `blood_quantity` varchar(20) DEFAULT '' COMMENT '输血数量',
  `blood_unit` varchar(20) DEFAULT '' COMMENT '输血单位',
  `blood_type` varchar(10) DEFAULT '' COMMENT '血型',
  `blood_rh` varchar(10) DEFAULT '' COMMENT 'RH血型',
  `blood_apply_code` varchar(20) DEFAULT '' COMMENT '申请单号',
  `blood_apply_date` datetime DEFAULT NULL COMMENT '申请时间',
  `blood_use_date` datetime DEFAULT NULL COMMENT '计划用血时间',
  `blood_comp_type_code` varchar(20) DEFAULT '' COMMENT '输血成分编码',
  `prepare_status` int(11) NOT NULL DEFAULT '0' COMMENT '备血状态 0.未开始 1.执行中 2.完成',
  `execute_date` datetime DEFAULT NULL COMMENT '备血完成时间',
  `execute_user_code` varchar(50) DEFAULT NULL COMMENT '备血完成执行人',
  `check_date` datetime DEFAULT NULL COMMENT '备血完成核对时间',
  `check_user_code` varchar(50) DEFAULT NULL COMMENT '备血完成核对人',
  `create_time` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `create_person` varchar(50) NOT NULL COMMENT '创建人',
  `update_time` datetime DEFAULT NULL COMMENT '修改时间',
  `update_person` varchar(50) DEFAULT '' COMMENT '修改人',
  PRIMARY KEY (`seq_id`),
  UNIQUE KEY `uq_unique_code` (`unique_code`),
  KEY `idx_inhos_code_apply_code` (`inhos_code`,`blood_apply_code`),
  KEY `idx_inhos_code_apply_date` (`inhos_code`,`blood_apply_date`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8 COMMENT='备血申请单表'
```

> [!danger] 注意事项
> 脚本后不要带 `;` ~

MongDB脚本

```js
// 无
```

# 3.提测单

>提测群

http://10.2.3.109:1024/zentao/testtask-view-8156.html
8260【规划】备血时，用血编号为空 --- 已提测
# 4.其它


遗留问题：

* 备血-采集取消后，PDA页面没有跳转


