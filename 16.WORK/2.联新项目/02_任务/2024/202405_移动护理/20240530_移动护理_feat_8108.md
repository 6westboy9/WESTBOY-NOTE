# 1.CR

>Java群

项目: mnis  
内容: 8108【佛山市中医院】护理记录单的自定义表头的可选项目，需要根据科室来维护
分支: feat_release_mnis_v3.8_wpb_8108 into qa_mnis_v3.8
MR:  http://10.2.3.111:86/backend_group/backend_application/mnis/merge_requests/5281

# 2.脚本

>邮件

```ad-error
注意提供脚本为实际<font color="#9bbb59">上线脚本</font>！

注意配置相关的默认值
 1. 开关，默认应为关闭
 2. IP地址，默认应为127.0.0.1
```

---

宝哥麻烦归档下脚本：

测试环境：10.2.3.170
项目：移动护理
服务：mnis
冲刺版本：移动护理迭代44
故事编号：8108
版本号：MNIS V3.8.98
分支：feat_release_mnis_v3.8_wpb_8108

MySQL脚本

```sql
use windranger_mnis;
CREATE TABLE IF NOT EXISTS `doc_template_item_custom_header_ward` (
  `seq_id` bigint(20) unsigned NOT NULL AUTO_INCREMENT COMMENT '自增长流水号',
  `template_code` varchar(20) NOT NULL COMMENT '模板编号',
  `item_code` varchar(20) NOT NULL COMMENT '项目编号',
  `ward_code` varchar(50) NOT NULL COMMENT '病区编号',
  `dic_code` varchar(50) NOT NULL COMMENT '字典编码',
  `dic_item_value` varchar(1000) NOT NULL COMMENT '字典项目',
  `create_person` varchar(50) NOT NULL COMMENT '创建人',
  `create_time` datetime NOT NULL COMMENT '创建时间',
  `update_person` varchar(50) DEFAULT NULL COMMENT '修改人',
  `update_time` datetime DEFAULT NULL COMMENT '修改时间',
  PRIMARY KEY (`seq_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8 COMMENT='文书模板项目病区表'
```


```ad-error
注意脚本后不要带 `;` ~
```

MongDB脚本

```js
// 无
```

# 3.提测单

>提测群

http://183.62.162.28:1024/zentao/testtask-view-7730.html
8108【佛山市中医院】护理记录单的自定义表头的可选项目，需要根据科室来维护 --- 已提测
# 4.其它

