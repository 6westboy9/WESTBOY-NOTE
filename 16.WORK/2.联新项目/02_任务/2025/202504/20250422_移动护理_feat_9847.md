## 1.CR

>Java群

项目：mnis 
内容：9847【湛江中心人民医院】-移动护理系统对接医信签CA，实现患者手机端和无线屏端签名
分支：feat_release_mnis_v3.8_wpb_9847 into release_mnis_v3.8
MR：http://10.2.3.111:86/backend_group/backend_application/mnis/merge_requests/6774
# 2.脚本

>邮件

> [!danger] 注意事项
> 提供脚本为实际线上脚本，所以必须考虑实际线上情况
> 1.开关，默认应为关闭（根据实际情况而定）
> 2.IP地址，默认应为127.0.0.1（一般情况）

---

宝哥麻烦归档下脚本：

测试环境：10.2.3.170
项目：mnis
冲刺版本：9847【湛江中心人民医院】-移动护理系统对接医信签CA，实现患者手机端和无线屏端签名
单号：9847
版本号：MNIS V3.8.134
分支：feat_release_mnis_v3.8_wpb_9847

MySQL脚本

```sql

```


> [!danger] 注意事项
> 脚本后不要带 `;` ~

MongDB脚本

```js
use coms
// db.sysConfigFile.find({'key':'CASetting'})
db.sysConfigFile.updateOne({ 'key': 'CASetting' }, { "$pull": { 'children': { 'key': { $in: ['signFileByQCCodeUrl', 'getFileCodeUrl'] } } } })
db.sysConfigFile.update({ 'key': 'CASetting' }, {
    "$push": {
        "children": {
            $each: [
                {
                    "_id": "17453186856921",
                    "key": "signFileByQCCodeUrl",
                    "pattern": {
                        "componentType": "TextArea",
                        "label": "云签代理-患者电子签名"
                    },
                    "value": ""
                },
                {
                    "_id": "17453259007102",
                    "key": "getFileCodeUrl",
                    "pattern": {
                        "componentType": "TextArea",
                        "label": "云签代理-获取文档签名"
                    },
                    "value": ""
                }]
        }
    }
})
```

# 3.提测单

>提测群

http://10.2.3.109:1024/zentao/testtask-view-9309.html
9847【湛江中心人民医院】-移动护理系统对接医信签CA，实现患者手机端和无线屏端签名

# 4.其它


```yaml
feign:
  client:
    config:
      default:
        connectTimeout: 10000 #设置feign客户端的链接超时
        readTimeOut: 10000 #设置feign客户端的读取超时时间
      shenZhenYunCa:
        logger-level: full
```


## 配置

1.配置文件

* 配置文件路径：`/config/attachment.properties`
* 配置项：signatureCallbackUrl=http://9eh3w8.natappfree.cc/callback
* 配置说明：电子签名回调地址（南宁二院的回调不了，走的是医院，湛江中心的可以）
* ![[Pasted image 20250423143621.png|400]]

2.配置中心





