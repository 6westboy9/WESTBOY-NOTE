## 1.CR  
  
>Java群  
  
项目：mnis  
内容：35629【广西南宁第二人民医院】增加统计报表-科室统计明显内容  
分支：fix_release_mnis_v3.8_wpb_35629 into release_mnis_v3.8  
MR：http://10.2.3.111:86/backend_group/backend_application/mnis/merge_requests/7087

# 2.脚本  
  
>邮件  
  
> [!danger] 注意事项  
> 提供脚本为实际线上脚本，所以必须考虑实际线上情况  
> 1.开关，默认应为关闭（根据实际情况而定）  
> 2.IP地址，默认应为127.0.0.1（一般情况）  
  
---  
  
宝哥麻烦归档下脚本：  
  
测试环境：10.2.3.170  
项目：mnis
单号：35629  
版本号：MNIS V3.8.139  
分支：fix_release_mnis_v3.8_wpb_35629 
  
MySQL脚本  
  
```sql  
USE windranger_mnis;
CREATE TABLE `exec_stats_fat_order_group_view` (  
  `seq_id` bigint(20) NOT NULL AUTO_INCREMENT COMMENT '自增长编号',  
  `id` varchar(30) NOT NULL COMMENT '缓存拆分医嘱id',  
  `report_id` varchar(30) NOT NULL COMMENT '报告id',  
  `report_ward_code` varchar(20) NOT NULL COMMENT '报告病区',  
  `report_date` datetime NOT NULL COMMENT '报告日期',  
  `group_seq_id` bigint(20) NOT NULL COMMENT '拆分医嘱主键id',  
  `group_unique_code` varchar(60) DEFAULT NULL COMMENT '批次唯一编号',  
  `order_group_no` varchar(50) NOT NULL DEFAULT '' COMMENT '医嘱批次编号',  
  `inhos_code` varchar(20) NOT NULL COMMENT '病人住院号',  
  `orderbar` varchar(100) NOT NULL DEFAULT '' COMMENT '医嘱条码',  
  `remark` varchar(100) DEFAULT '' COMMENT ' 医嘱备注',  
  `execute_status` int(11) DEFAULT NULL COMMENT '执行状态  0 - 未执行， 1- 执行中， 2 - 已执行， 3-停止，4-作废',  
  `execute_type` int(11) DEFAULT NULL COMMENT '执行方式 0-无效， 1- 扫描执行， 2 - 手动执行',  
  `order_seq_id` bigint(20) DEFAULT NULL COMMENT '原始医嘱主键id',  
  `order_code` varchar(50) DEFAULT NULL COMMENT '医嘱号',  
  `pat_code` varchar(20) DEFAULT NULL COMMENT '病人标识号',  
  `order_sub_no` varchar(16) DEFAULT '' COMMENT '医嘱子序号',  
  `repeat_indicator` int(16) DEFAULT NULL COMMENT '长期医嘱标志(1-长期，0-临时)',  
  `order_class_code` varchar(30) DEFAULT NULL COMMENT '医嘱类型编号',  
  `order_class` varchar(16) DEFAULT '' COMMENT '医嘱类型名称',  
  `order_text` varchar(1024) DEFAULT '' COMMENT '医嘱正文',  
  `dosage` decimal(11,5) DEFAULT '0.00000' COMMENT '药品一次使用剂量',  
  `dosage_units` varchar(512) DEFAULT '' COMMENT '剂量单位',  
  `administration_code` varchar(30) DEFAULT '' COMMENT '用法编号',  
  `administration` varchar(16) DEFAULT '' COMMENT '给药途径和方法',  
  `frequency_code` varchar(30) DEFAULT '' COMMENT '频率编号',  
  `frequency` varchar(16) DEFAULT '' COMMENT '执行频率描述',  
  `order_status` varchar(16) DEFAULT '' COMMENT '医嘱状态(新开、校对、执行、停止等)',  
  `source_name` varchar(10) DEFAULT '' COMMENT '数据来源',  
  `process_seq_id` bigint(20) DEFAULT NULL COMMENT '医嘱执行流程主键id',  
  `exec_ward_code` varchar(20) DEFAULT '' COMMENT '执行病区（如果没有执行取开立病区，如果执行了取执行病区）',  
  `exec_time` datetime DEFAULT NULL COMMENT '执行时间（如果没有执行取计划时间，如果执行了取执行时间）',  
  `exec_nurse` varchar(50) DEFAULT NULL COMMENT '执行护士',  
  `exec_type_code` varchar(20) DEFAULT '' COMMENT '医嘱用法组',  
  `order_category_code` varchar(20) DEFAULT '' COMMENT '医嘱类别组',  
  `cache_date` datetime DEFAULT NULL COMMENT '缓存日期',  
  `status_id` int(11) DEFAULT NULL COMMENT '状态ID',  
  `status_desc` varchar(20) DEFAULT NULL COMMENT '状态描述',  
  `last_record_execute_date` datetime DEFAULT NULL COMMENT '最近一次执行记录执行时间',  
  `last_record_display_name` varchar(20) DEFAULT '' COMMENT '最近一次执行记录展示人名',  
  `last_record_execute_name` varchar(20) DEFAULT '' COMMENT '最近一次执行记录执行人名',  
  `last_record_execute_content` varchar(255) DEFAULT '' COMMENT '最近一次执行记录执行内容',  
  `last_record_remark` varchar(255) DEFAULT '' COMMENT '最近一次执行记录执行备注',  
  PRIMARY KEY (`seq_id`)  
) ENGINE=InnoDB DEFAULT CHARSET=utf8 COMMENT='医嘱执行统计明细视图'
```  
  
  
> [!danger] 注意事项  
> 脚本后不要带 `;` ~  
  
MongDB脚本  
  
```js  
use coms
db.sysConfigFile.deleteOne({ 'key': 'deptStats' })
db.sysConfigFile.insertOne({
	"_id" : ObjectId("686f9726587e2202232b4f55"),
	"key" : "deptStats",
	"pattern" : {
		"componentType" : "Tab",
		"label" : "科室统计",
		"forAllWards" : true
	},
	"children" : [
		{
			"_id" : "686f9375587e2202232b4f54-Thu Jul 10 2025 18:18:42 GMT+0800",
			"key" : "orderExecStatsStorageType",
			"pattern" : {
				"componentType" : "Radio",
				"label" : "医嘱执行统计明细存储类型",
				"urlOptionSourceDetail" : {
					
				},
				"optionSourceDetail" : "",
				"optionSource" : "options",
				"options" : [
					{
						"sortedIndex" : 1,
						"code" : "0",
						"name" : "不存储"
					},
					{
						"sortedIndex" : 2,
						"code" : "1",
						"name" : "明细"
					},
					{
						"sortedIndex" : 3,
						"code" : "2",
						"name" : "明细+视图"
					}
				],
				"optionKeyAttrs" : [
					{
						"horizontalLayout" : true,
						"componentType" : "Checkbox",
						"enableSelectAll" : true
					}
				]
			},
			"value" : "1"
		}
	],
	"relatedContents" : [ "statisticalConfig" ],
	"updatePerson" : "123",
	"updateTime" : ISODate("2025-07-10T19:15:59.306+08:00"),
	"createTime" : ISODate("2025-07-10T18:34:14.117+08:00"),
	"createPerson" : "123",
	"_class" : "com.lachesis.windranger.foundation.model.config.SysConfigFile"
})
```  
  
# 3.提测单  
  
>提测群  
  
紧急插入需求，线上已验证：35629【广西南宁第二人民医院】增加统计报表-科室统计明显内容 
http://183.62.162.28:1024/zentao/testtask-view-9673.html#app=execution
  
# 4.其它