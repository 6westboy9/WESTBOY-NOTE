# 1.CR

>Java群

项目：mnis
内容：9976【清华大学附属北京清华长庚医院】-检验采集结束后弹框录入24小时尿量
分支：feat_release_mnis_v3.8_wpb_9976 into release_mnis_v3.8
MR：http://10.2.3.111:86/backend_group/backend_application/mnis/merge_requests/6883

# 2.脚本

>邮件

> [!danger] 注意事项
> 提供脚本为实际线上脚本，所以必须考虑实际线上情况
> 1.开关，默认应为关闭（根据实际情况而定）
> 2.IP地址，默认应为127.0.0.1（一般情况）

---

宝哥麻烦归档下脚本：

测试环境：10.2.3.170
项目：mnis
冲刺版本：移动护理迭代66
单号：9976
版本号：MNIS V3.8.136
分支：feat_release_mnis_v3.8_wpb_9976

MySQL脚本

```sql

```


> [!danger] 注意事项
> 脚本后不要带 `;` ~

MongDB脚本

```js
use coms

db.sysConfigFile.update({ 'key': 'orderExecutionConfig' }, { "$pull": { 'children': { 'key': 'urineProtein24hOrderName' } } })
db.sysConfigFile.update({ 'key': 'orderExecutionConfig' }, {
    "$push": {
        "children":
        {
            "_id": "65095ee1cf1fe2309d010848-Thu May 22 2025 13:43:27 GMT+0800",
            "key": "urineProtein24hOrderName",
            "pattern": {
                "componentType": "Input",
                "label": "24h尿蛋白定量匹配医嘱名称"
            }
        }
    }
})

// 补充脚本（9944【兴安界首骨伤医院】-移动护理北京云签的ca配置支持）
(function() {
    function addOption(setting) {
        var caSetting = db.sysConfigFile.findOne({ key: setting, wardCode: '' })
        if (caSetting == null) {
            return
        }
        var children = caSetting.children
        var update = false
        if (Array.isArray(children) && children.length > 0) {
            for (var i = 0; i < children.length; i++) {
                var child = children[i]
                if (child.key === 'caProvider') {
                    var options = child.pattern.options
                    var exists = false
                    var optionsLength = options.length
                    for (var j = 0; j < optionsLength; j++) {
                        var option = options[j]
                        if (option.code === 'ca51Trust') {
                            exists = true
                            break
                        }
                    }

                    print(setting, optionsLength, exists)

                    if (!exists) {
                        var item = {
                            "sortedIndex": optionsLength,
                            "code": "ca51Trust",
                            "name": "数字医信"
                        }
                        print("不存在进行添加: " + JSON.stringify(item))
                        options.push(item)
                        update = true
                    }
                }
            }
        }

        if (update) {
            db.sysConfigFile.save(caSetting)
        }
    }

    addOption('CASettingSub');
    addOption('CASetting');
})();
```

# 3.提测单

>提测群

9976【清华大学附属北京清华长庚医院】-检验采集结束后弹框录入24小时尿量
http://10.2.3.109:1024/zentao/testtask-view-9455.html



# 4.其它
