
* 已归档：2026-01-20 在[[20260114_移动护理_feat_11227（81提测）]]中已经补充

```sql
use windranger_mnis;  
create index idx_inhos_code_document_emplate on doc_data_evaluate_header(inhos_code,document_code,template_code);  
create index idx_inhos_code_document_emplate on doc_data_ordinary(inhos_code,document_code,template_code);
```


# 20260120

* 问题单：38444【广西科技大学第一附属医院】-手术交接模块点击接收报错 `待归档`

```SQL
use windranger_mnis;  
create table if not exists pat_inhos_surgery_handover  
(  
    seq_id         bigint unsigned auto_increment comment '自增长流水号'  
        primary key,  
    surgery_code   varchar(20)            not null comment '手术编号',  
    inhos_code     varchar(20) default '' not null comment '住院号(住院号码)',  
    receive_person varchar(20)            not null comment '接收人',  
    receive_time   datetime               not null comment '接收时间',  
    receive_ward   varchar(20) default '' null comment '接收病区',  
    create_person  varchar(20)            not null,  
    create_time    datetime               not null on update CURRENT_TIMESTAMP  
) comment '手术交接信息';
```